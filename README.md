# Dartboard SVG App

A minimal Next.js app that renders a realistic dartboard SVG with standard colors and number ring.

Run locally:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```

Deploy (Vercel):

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-5a61119a
```
